///
/// HybridWriterSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `Metadata` to properly resolve imports.
namespace margelo::nitro::writer { struct Metadata; }

#include <NitroModules/Promise.hpp>
#include <string>
#include "Metadata.hpp"
#include <optional>

namespace margelo::nitro::writer {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Writer`
   * Inherit this class to create instances of `HybridWriterSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridWriter: public HybridWriterSpec {
   * public:
   *   HybridWriter(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridWriterSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridWriterSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridWriterSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<void>> writeMetadata(const std::string& input, const std::string& output, const Metadata& metadata, const std::optional<std::string>& artwork) = 0;
      virtual std::shared_ptr<Promise<Metadata>> readMetadata(const std::string& input) = 0;
      virtual std::shared_ptr<Promise<void>> clearMetadata(const std::string& input, const std::string& output) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Writer";
  };

} // namespace margelo::nitro::writer
